# Execution log — production infrastructure plan

| Date | Task | Summary | Verification | Commit | Deviations |
| --- | --- | --- | --- | --- | --- |
| 2026-08-22 | 0 | Snapshot backup branch pushed as `pre-infra-backup-2026-08-22`; execution log started. | `git ls-remote` confirms branch on origin. | (this commit) | Plan named the branch `backup/pre-infra-2026-08-22`, but the remote already has a branch literally named `backup`, so `backup/*` refs are rejected ("directory file conflict"). Renamed to `pre-infra-backup-2026-08-22`. Also, the repo has no `main` branch — the backup snapshots the base branch `v0/production-infrastructure-plan-80984c7d` instead. |
| 2026-08-22 | 1 | Version-verified decision: installed `next@16.2.12`; `@opennextjs/cloudflare` `proxy.ts` support is EXPERIMENTAL only (PRs #1309/#1320, issue #1279 — build warning, not production-supported). Applied the NOT-supported branch: `git mv src/proxy.ts src/middleware.ts`, renamed export `proxy` → `middleware`, file header records the adapter constraint. Test import updated. Exactly one interception file exists. | `pnpm typecheck` clean; `pnpm test` 121 files / 1182 tests pass. Smoke-test criteria (5 items) defined in plan Task 1 Step 4 — executed against the Task 7 preview deploy. | (task-1 commit) | None. |
| 2026-08-22 | 2 | DB/auth boundary baseline (ADR-002 Phase 0) complete end-to-end. Adapter: `src/lib/db/{client,sql,transaction,errors}.ts` (postgres-js, Hyperdrive-aware resolution, parameterized-only, no observability side effects). Facade: `src/lib/auth-provider/index.ts`. Migration `20260822120000_current_app_user_id.sql`: portability shim + `is_account_member` refactor (live definition extracted per plan hard rule, `SECURITY DEFINER` restated, only `auth.uid()` → `current_app_user_id()` changed). Boundary check extended with 153-importer baseline (new direct `@supabase/*` imports fail; baselined warn). Migration APPLIED to the development DB: `pnpm db:push` → "Schema is current: 135 migrations found, 104 applied"; `pnpm db:doc` regenerated (90 tables, 194 functions, 249 policies); `pnpm docs:sync` clean. | `pnpm check` all green: typecheck + lint (1 pre-existing warning) + boundaries (153/153 baseline) + docs sync + 121 files / 1182 tests pass. | d33ca00 (code) + this commit (migration apply/doc refresh) | Initial `db:push` replay failed at pre-existing migration `032_fix_ai_knowledge_membership.sql`: the function-security invariant checker (added later in `push-supabase-schema.mjs`) flagged its deliberate DEFINER→INVOKER downgrade of `match_ai_knowledge_fts`/`match_ai_knowledge_semantic` (the security fix that migration exists for). Added the checker's own comment-only annotation `-- wacrm:allow-security-downgrade <fn>` to 032 — no SQL semantics changed; live schema already reflects INVOKER. |
| 2026-08-22 | 4 | Cloudflare/OpenNext scaffolding complete. `wrangler.jsonc` (name `auxelon-app`, `nodejs_compat`, smart placement, observability logs, cron `0 0 * * *` copied from the removed `vercel.json`, ONE `HYPERDRIVE` binding with id deferred to Task 11), `open-next.config.ts`, `.env.production.example` manifest, dual-mode `scripts/check-env-completeness.mjs` (`--manifest` on PRs, `--runtime` only in the promotion job). `@opennextjs/cloudflare` + `wrangler` added as devDependencies; `vercel.json` deleted; no `@vercel/analytics`/`@vercel/speed-insights` packages remained to remove. `check:env` wired into `pnpm check`. | `pnpm check` green; OpenNext build produces `.open-next/worker.js`. | 5d6b685 (PR #326) | Log entry written retroactively — code landed before this entry. |
| 2026-08-22 | 5 | Health endpoints complete. `/api/health` liveness (no I/O, returns release + git SHA — NFR-002) and `/api/health/dependencies` readiness (Supabase `SELECT 1` + Redis PING with 1.5 s timeouts; public payload is name/ok/ms only, raw errors go to the logger — review §12; 503 when degraded). | `pnpm check` green; both endpoints return expected shapes locally. | d0b3537 (PR #327) | Realtime facade landed in this commit (a Task 6 deliverable pulled forward). Facade exposes `getChannel`/`removeChannel` rather than the plan's single `subscribe()` signature — the 4 call sites use 3 different Supabase primitives (postgres_changes, presence, broadcast); vendor import is still confined to one file, which is the actual requirement. |
| 2026-08-22 | 6 | Observability + cache adapters complete. `src/lib/observability/{correlation,logger,errors,instrument,langfuse}.ts`: correlation vocabulary, pino logger with PII redaction + env-gated Loki push (fire-and-forget, NFR-003), Sentry facade (no-op without `SENTRY_DSN`), functional decorators + slow-query instrumentation wrapping the sql adapter, Langfuse tracing decorator with explicit PII policy (prompt/completion text never stored raw). `src/lib/cache/{keys,redis-cache}.ts`: read-through Upstash cache with tenant-scoped key allowlist (every key starts `['account', accountId]`), transparent-miss fallback when Redis absent. `/api/vitals` + `analytics-provider.tsx` (Cloudflare Web Analytics and custom vitals documented as two separate systems — review §15). | `pnpm check` green including cache key tests. | 6889354 (PR #328) | Log entry written retroactively. |
| 2026-08-22 | 3 | Reliability ports complete (NFR-004/005/008). Ports: `src/lib/ports/{message-ingress,concurrency-guard,ai-provider}.ts` (vendor-free interfaces per Dependency Rule). Migration `20260822130000_webhook_event_dedupe.sql`: `webhook_events(event_id pk, account_id, processed_at)` + TTL cleanup index. Webhook route now: verify signature → `INSERT ... ON CONFLICT DO NOTHING` claim → duplicate = 200 fast → `SynchronousMessageIngress.accept()` wrapping the existing pipeline verbatim (Flows → Automations → AI precedence unchanged). Bulkhead adapter `src/lib/concurrency-guard.ts` (Upstash-backed, fail-open) wired around the AI auto-reply path only. `AIProvider` strategy rule recorded; existing `lib/ai/providers/` left as-is (no aesthetic refactor). §A–§G addendum recorded in `.agents/context/hld.md` §10. | `pnpm check` all green: typecheck, lint (1 pre-existing warning), boundaries (153/153 baseline), docs sync, 121 files / 1182 tests. | 0d53942 + 4277441 | Log entry written retroactively — Task 3 code landed via PR #325 before this entry was appended. |
| 2026-08-22 | 7 | CI/CD pipeline complete — 7 workflows: `ci.yml` (typecheck/lint/boundaries/docs/env-manifest/tests + OpenNext build artifact), `preview-deploy.yml` (Cloudflare preview per PR), `promote-to-prod.yml` (eligibility gate: verified CI artifact for the EXACT sha, db-migrate success, manual dispatch with sha input; release manifest; post-deploy health poll with auto-rollback), `rollback-production.yml` (redeploy last-known-good manifest), `db-migrate.yml` (migrations gated separately, invariant checker enforced), `security.yml` (CodeQL + dependency review), `ai-review.yml`. All third-party actions SHA-pinned; concurrency groups prevent overlapping prod deploys. | `actionlint` 1.7.12 clean on all 7 workflows; `pnpm check` all green (typecheck, lint, boundaries, docs sync, env manifest, 121 files / 1182 tests). | a20d87e (PR #329) + this commit | Log entry written retroactively. Verification uncovered a half-migrated realtime call site: `use-team-chat.ts` used `getChannel` without importing the Task 6 facade and its cleanup referenced an out-of-scope `supabase` — fixed by importing `getChannel`/`removeChannel`/`RealtimeChannel` from `@/lib/realtime` (typecheck was red at HEAD before this fix). |
