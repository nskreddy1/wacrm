// OpenNext → Cloudflare Workers adapter config (plan Task 4).
// Defaults only — incremental cache / queue overrides are added when a
// measured requirement appears, never speculatively.
import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({});
