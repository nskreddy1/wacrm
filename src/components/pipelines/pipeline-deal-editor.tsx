"use client"

import { useState } from "react"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { ScrollArea } from "@/components/ui/scroll-area"
import { Select, SelectContent, SelectGroup, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Sheet, SheetContent, SheetDescription, SheetFooter, SheetHeader, SheetTitle } from "@/components/ui/sheet"
import { Textarea } from "@/components/ui/textarea"
import type { ActionResult } from "@/lib/pipelines/actions"
import type { PipelineDeal, PipelineSnapshot } from "@/lib/pipelines/domain"
import type { DealInput } from "@/lib/pipelines/validation"
import { cn } from "@/lib/utils"

function draftFrom(deal: PipelineDeal | null, snapshot: PipelineSnapshot, stageId: string): DealInput {
  return { id: deal?.id, pipelineId: snapshot.pipeline.id, stageId: deal?.stageId ?? stageId, contactId: deal?.contactId ?? null, assignedTo: deal?.assignedTo ?? null, title: deal?.title ?? "", value: deal?.value ?? 0, currency: deal?.currency ?? "USD", company: deal?.company ?? null, priority: deal?.priority ?? "normal", probability: deal?.probability ?? 20, source: deal?.source ?? null, activity: deal?.activity ?? null, nextStep: deal?.nextStep ?? null, description: deal?.description ?? null, due: deal?.due ?? null, status: deal?.status ?? "open", position: deal?.position ?? 0 }
}

export function PipelineDealEditor({ open, deal, defaultStageId, snapshot, pending, onOpenChange, onSave }: { open: boolean; deal: PipelineDeal | null; defaultStageId: string; snapshot: PipelineSnapshot; pending: boolean; onOpenChange: (open: boolean) => void; onSave: (input: DealInput) => Promise<ActionResult<PipelineDeal>> }) {
  const [draft, setDraft] = useState(() => draftFrom(deal, snapshot, defaultStageId))
  const [error, setError] = useState("")
  async function submit() { const result = await onSave(draft); if (!result.ok) setError(result.error) }
  return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent className="sm:max-w-xl"><SheetHeader><SheetTitle>{deal ? deal.title : "Create deal"}</SheetTitle><SheetDescription>Manage the opportunity, owner, value, and next action.</SheetDescription></SheetHeader><ScrollArea className="h-[calc(100vh-10rem)]"><div className="grid gap-4 px-4 py-2 sm:grid-cols-2"><Field label="Deal name" wide><Input value={draft.title} onChange={(event) => setDraft({ ...draft, title: event.target.value })} aria-invalid={Boolean(error)} /></Field><Field label="Amount"><Input type="number" min="0" value={draft.value} onChange={(event) => setDraft({ ...draft, value: Number(event.target.value) })} /></Field><Field label="Currency"><Input maxLength={3} value={draft.currency} onChange={(event) => setDraft({ ...draft, currency: event.target.value.toUpperCase() })} /></Field><Field label="Stage"><Select value={draft.stageId} onValueChange={(value) => value && setDraft({ ...draft, stageId: value })}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent><SelectGroup>{snapshot.stages.map((stage) => <SelectItem key={stage.id} value={stage.id}>{stage.name}</SelectItem>)}</SelectGroup></SelectContent></Select></Field><Field label="Closing date"><Input type="date" value={draft.due ?? ""} onChange={(event) => setDraft({ ...draft, due: event.target.value || null })} /></Field><Field label="Contact"><Select value={draft.contactId ?? "none"} onValueChange={(value) => value && setDraft({ ...draft, contactId: value === "none" ? null : value })}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="none">No contact</SelectItem>{snapshot.contacts.map((contact) => <SelectItem key={contact.id} value={contact.id}>{contact.name}</SelectItem>)}</SelectGroup></SelectContent></Select></Field><Field label="Owner"><Select value={draft.assignedTo ?? "none"} onValueChange={(value) => value && setDraft({ ...draft, assignedTo: value === "none" ? null : value })}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent><SelectGroup><SelectItem value="none">Unassigned</SelectItem>{snapshot.members.map((member) => <SelectItem key={member.id} value={member.id}>{member.name}</SelectItem>)}</SelectGroup></SelectContent></Select></Field><Field label="Company"><Input value={draft.company ?? ""} onChange={(event) => setDraft({ ...draft, company: event.target.value || null })} /></Field><Field label="Probability"><Input type="number" min="0" max="100" value={draft.probability} onChange={(event) => setDraft({ ...draft, probability: Number(event.target.value) })} /></Field><Field label="Next step" wide><Input value={draft.nextStep ?? ""} onChange={(event) => setDraft({ ...draft, nextStep: event.target.value || null })} /></Field><Field label="Description" wide><Textarea value={draft.description ?? ""} onChange={(event) => setDraft({ ...draft, description: event.target.value || null })} /></Field>{error && <p role="alert" className="text-sm text-destructive sm:col-span-2">{error}</p>}</div></ScrollArea><SheetFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button><Button disabled={pending || !draft.title.trim()} onClick={() => void submit()}>{pending ? "Saving…" : "Save deal"}</Button></SheetFooter></SheetContent></Sheet>
}
function Field({ label, wide, children }: { label: string; wide?: boolean; children: React.ReactNode }) { return <div className={cn("flex flex-col gap-2", wide && "sm:col-span-2")}><Label>{label}</Label>{children}</div> }
